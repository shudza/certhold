package peerfiles

// ManagerPrincipal is the implicit principal carried by every peer cert and
// authorized_keys cert-authority line, letting the certhold manager SSH in.
const ManagerPrincipal = "manager"
